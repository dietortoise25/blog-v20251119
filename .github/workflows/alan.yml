name: lsj

on: push

jobs:
  npm-build:
    name: npm build
    runs-on: ubuntu-latest

    steps:
      - name: 读取仓库内容
        uses: actions/checkout@v4
      - name: 安装依赖
        run: pnpm install
      - name: 构建项目
        run: pnpm run build
